#pragma once

// Wi-Fi (2.4 GHz only)
#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Backend URL (no trailing slash). 本番は Railway のカスタムドメイン等。
// 例: https://auto-irrigation-demo-production.up.railway.app
#define SERVER_URL    "https://your-backend.up.railway.app"
